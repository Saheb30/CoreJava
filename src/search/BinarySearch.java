package search;

public class BinarySearch {

	static int search(int[] arr, int element) {
		if(arr.length == 0) {
			System.out.println("invalid input: There is no element in array");
		}
		int low = 0;
		int high = arr.length-1;
		int mid = low+(high-low)/2;
		
		return 0;
	}
}
